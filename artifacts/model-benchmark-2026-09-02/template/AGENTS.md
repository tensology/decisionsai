# Benchmark instructions

- Modify only `task.py`.
- Run the supplied unit tests before reporting completion.
- Prefix shell commands with `rtk` when RTK supports the command.
- Do not use the network.
