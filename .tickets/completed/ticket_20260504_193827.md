---
id: ticket_20260504_193827
title: Improve project routing and TTS response hygiene
project: DecisionsAI
created: 2026-05-04 19:38:27
status: done
---

## Description
Enhance project-mode conversational responses across all projects, with a specific guardrail that project-targeted actions (for example, “create ticket in DecisionsAI”) must execute in the explicitly requested project even when another project is currently active.

Problem summary:
- Project context can be applied incorrectly when the user explicitly names a target project, causing work to be created in the wrong project.
- Spoken responses can sound unnatural (filler interjections, elongated phrasing, rambling).
- Spoken output can include internal/reference/tool text that should remain non-spoken.
- Some flows can produce duplicated verbal output (conversational summary + raw/tool block spoken again).

Goal:
Make project-mode and project-adjacent conversations crisp, confident, and accurate across all projects, with deterministic project targeting and clean TTS output.

Clipboard context (normalized):
- Title: Enhance Project-Mode Conversational Responses
- Instruction: Create/update a ticket in DecisionsAI `.tickets` to improve TTS output, integrate logs into the ticket, and improve contextual accuracy.

## Requirements
- **Context handling**
  - If the user specifies a target project (e.g., `DecisionsAI`), create the ticket in that project regardless of currently active project.
  - If needed, switch active project first, then create the ticket, and confirm the switch in one short sentence.
  - Add deterministic routing precedence: explicit project mention in user request must override ambient/active project context.

- **TTS output quality**
  - Strip filler interjections and hesitation phrases (e.g., “I don’t know”, “ahhh”) from confirmations unless semantically required.
  - Reduce elongated/“wrung-out” speech artifacts by normalizing punctuation and response phrasing before TTS.
  - Keep most confirmations to 1–3 clear sentences.

- **Output hygiene**
  - Never speak internal blocks (`REFERENCE:` sections, debug payloads, or raw tool text).
  - Ensure one spoken response per action; do not speak both a conversational summary and raw tool output.

- **Consistency and duplication prevention**
  - Add guardrails so TTS consumes only the designated conversational/voice channel.
  - Prevent duplicate enqueues/emits of the same confirmation event in the chat+voice pipeline.

- **Logging and diagnostics**
  - Append relevant recent logs and/or conversation trace to each incident ticket with:
    - timestamp window (start/end),
    - source path,
    - concise extracted snippet.
  - Implement reliable fallback log discovery:
    1. Detect active logger file handler path.
    2. Fallback to `DB_DIR/logs/decisions.log`.
    3. If no file yet, include an explicit “no log file yet” diagnostic note.
  - Document log path configurability and extraction behavior used by ticket tooling.

- **Acceptance criteria**
  - A ticket request for `DecisionsAI` lands in `DecisionsAI/.tickets` even if another project is active.
  - Spoken confirmations are brief, natural, and free of filler/elongation artifacts.
  - Internal/debug/reference blocks are never spoken.
  - No duplicated spoken confirmations for a single action.
  - Ticket includes logs/trace with timestamp window and source path.

## Context
- **Project:** DecisionsAI
- **Folder:** /Users/paul/development/TENSOLOGY/DECISIONS/DecisionsAI/


## Related Files
- `/Users/paul/development/TENSOLOGY/DECISIONS/DecisionsAI/distr/core/agent/tools/integrations/kanban_ticket.py` (project ticket routing / write target)
- `/Users/paul/development/TENSOLOGY/DECISIONS/DecisionsAI/distr/core/agent/tool_voice_format.py` (`REFERENCE:` separation for voice vs technical output)
- `/Users/paul/development/TENSOLOGY/DECISIONS/DecisionsAI/distr/core/agent/tool_audit.py` (agent tool audit logging to `decisions.log`)
- `/Users/paul/development/TENSOLOGY/DECISIONS/DecisionsAI/distr/gui/web/routes/settings/logs.py` (log path detection + fallback to `DB_DIR/logs/decisions.log`)
- `/Users/paul/development/TENSOLOGY/DECISIONS/DecisionsAI/db/logs/decisions.log` (runtime log source)

## Conversation Context
- User requested ticket creation/update for the **DecisionsAI** project with cross-project context correctness.
- User highlighted TTS quality issues: filler words, unnatural elongated speech, and rambling confirmations.
- User highlighted output hygiene issues: internal/reference/tool output being spoken and duplicate spoken responses.
- User requested logs to be appended with clear timestamp window and source path, including fallback behavior when log location is configurable.

## Recent Logs (appended)
Source: `/Users/paul/development/TENSOLOGY/DECISIONS/DecisionsAI/db/logs/decisions.log`  
Window sampled: `2026-05-04 19:59:48` to `2026-05-04 20:10:46`

```log
2026-05-04 20:04:50,779 - distr.core.agent.tools.loader - INFO - load_tools called with use_navigation_tools=False
2026-05-04 20:04:50,780 - distr.core.agent.tools.loader - WARNING - use_navigation_tools is False - skipping specialized tools!
2026-05-04 20:04:50,780 - distr.core.agent.tools.loader - INFO - Loaded 8 total tools
2026-05-04 20:09:53,292 - distr.core.agent.tools.loader - INFO - load_tools called with use_navigation_tools=False
2026-05-04 20:09:53,293 - distr.core.agent.tools.loader - WARNING - use_navigation_tools is False - skipping specialized tools!
2026-05-04 20:09:53,293 - distr.core.agent.tools.loader - INFO - Loaded 8 total tools
```

Log capture notes:
- Primary resolution strategy should match app behavior in `settings/logs.py`: discover active logger file handler path first.
- Fallback path is `DB_DIR/logs/decisions.log`.
- If no log file exists yet, ticket should include a structured note with attempted path and “no log file yet” status instead of failing silently.

## Implementation Audit (2026-05-05)
### Completed in codebase
- Explicit project override is now project-agnostic in `create_project_ticket`: target project is resolved from DB project names + trigger words, then activated before ticket write.
- Ambiguity handling is now project-agnostic: when multiple explicit project mentions are detected, the assistant asks one short clarifying question.
- Voice/reference separation exists via `voice_then_reference(...)` and `clean_text_for_tts(...)` strips everything after `REFERENCE:`.
- TTS hygiene improvements are present: filler-word removal and elongated-letter normalization in `clean_text_for_tts(...)`.
- Log fallback behavior exists in settings route: active logger handler path first, then `DB_DIR/logs/decisions.log`, plus explicit no-log-file diagnostic response.
- Duplicate spoken confirmation guardrail exists in `speak_text_directly_event_queue(...)` with short-burst dedup protection.
- Automatic evidence attachment is wired into workflow and CLI ticket writeback (log source + timestamp window + snippet/no-log diagnostic).
- Deterministic tests were added for project-target precedence and TTS duplicate suppression.

### Outstanding work for new system rollout
- None blocking for this ticket scope.

### Optional follow-ups
- Add full pipeline E2E tests that verify no duplicate spoken confirmations in live chat+voice orchestration.
- Add mixed-tool-output regression coverage that validates `REFERENCE:` tails are never emitted to the spoken channel.

---
*Auto-generated by DecisionsAI*
